{
  "name": "facerix-template",
  "version": "1.0.0",
  "description": "Template for Facerix Progressive Web Apps",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "start": "live-server --no-browser --port=8080",
    "test": "node --test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "author": "Rylee Corradini",
  "license": "GPL-3.0",
  "devDependencies": {
    "live-server": "^1.2.2",
    "oxlint": "^0.15.0",
    "prettier": "^3.0.0"
  }
}
